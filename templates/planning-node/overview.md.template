# Overview — node `{{NODE_ID}}` — {{TITLE}}

## Intent

What problem this slice solves; link to roadmap node `{{NODE_ID}}`.

## User-visible behavior

-

## Out of scope

-

## References

- Roadmap: `roadmap/manifest.json` (`includes`) → chunk file for `{{NODE_ID}}`
- Contracts (read selectively): `shared/`
