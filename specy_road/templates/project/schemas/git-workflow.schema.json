{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://specy-road.local/schemas/git-workflow.schema.json",
  "title": "specy-road git workflow contract",
  "type": "object",
  "required": ["version", "integration_branch", "remote"],
  "additionalProperties": false,
  "properties": {
    "version": { "type": "integer", "const": 1 },
    "integration_branch": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[A-Za-z0-9._/-]+$",
      "description": "Daily integration trunk (e.g. main, dev)."
    },
    "remote": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[A-Za-z0-9._-]+$",
      "description": "Git remote name (usually origin)."
    },
    "merge_request_requires_manual_approval": {
      "type": "boolean",
      "description": "When true, merge requests require human approval before merge; CLI may print reminders."
    },
    "require_implementation_review_before_finish": {
      "type": "boolean",
      "description": "When true, finish-this-task requires a human-approved implementation review in registry.yaml and work/implementation-summary-<NODE_ID>.md before closing the task."
    },
    "require_implementation_before_finish": {
      "type": "boolean",
      "description": "When true (default if omitted), finish-this-task refuses to close a node whose branch changes no file under its declared touch_zones. Skipped when the node declares no zones, or when git cannot compute the diff. CLI --allow-empty-implementation overrides it per run."
    },
    "cleanup_work_artifacts_on_finish": {
      "type": "boolean",
      "description": "When true (default if omitted), finish-this-task removes work/brief-, work/prompt-, and work/implementation-summary- for the node after validate/export."
    },
    "on_complete": {
      "type": "string",
      "enum": ["auto", "merge", "pr"],
      "description": "How finish-this-task lands work: pr = open a PR/MR (default); merge = merge feature branch into integration locally and push; auto = try merge+push first, then suggest PR/MR on failure."
    },
    "grind_session_max_limit_waits": {
      "type": "integer",
      "minimum": 0,
      "description": "grind-session: how many times one leaf may wait out a Claude usage limit before the run stops (default 3). CLI --max-limit-waits wins."
    },
    "grind_session_max_limit_wait_hours": {
      "type": "number",
      "minimum": 0,
      "description": "grind-session: longest single wait for a stated Claude reset, in hours (default 6). A reset further out stops the run rather than waiting unattended. CLI --max-limit-wait-hours wins."
    },
    "grind_session_limit_wait_grace_seconds": {
      "type": "integer",
      "minimum": 0,
      "description": "grind-session: slack added after the stated reset, because clocks disagree (default 120). CLI --limit-wait-grace-seconds wins."
    },
    "grind_session_max_empty_retries": {
      "type": "integer",
      "minimum": 0,
      "description": "grind-session: how many times to re-run a Claude implement command that died on a signal without printing anything (default 2). Not a retry for failed implementations - those still stop the run. CLI --max-empty-retries wins."
    }
  }
}
