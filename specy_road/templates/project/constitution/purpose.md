# Purpose

**Replace with your product or program name** — describe *why* this effort exists and what success looks like in plain language.

This file is **human judgment** (not machine-validated). Enforceable rules belong under `constraints/`; the roadmap **graph** belongs under `roadmap/` JSON; feature **sheets** belong under flat `planning/*.md` per node with `planning_dir`.
