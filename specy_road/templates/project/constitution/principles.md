# Principles

1. **Contracts over tribal knowledge** — cite `shared/` specs from roadmap work items.
2. **Roadmap-first** — the merged graph under `roadmap/` is canonical for priorities and dependencies.
3. **Separation of concerns** — purpose here; enforceable limits in `constraints/`.

Replace or extend these for your team.
