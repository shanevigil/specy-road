# Shared contracts

Place specifications, API shapes, and policies here. Roadmap nodes should **cite** these paths in `agentic_checklist.contract_citation` instead of duplicating intent in chat.

Add Markdown or other tracked artifacts as your project grows.
