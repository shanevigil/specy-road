# specyrd (optional IDE glue)

This directory is created by **`specyrd init`** from the **specy-road** kit. It records which agent packs were installed; it is **not** a second source of truth for roadmap rules.

- **Canonical workflow** remains the **specy-road** CLI (`specy-road validate`, `brief`, `export`, …) and `python scripts/…` — see the repo [`README.md`](../README.md).
- **Not Spec Kit:** `specyrd` is unrelated to the Spec Kit **`specify`** CLI. Optional milestone folders named `specify/<node-id>/` in this kit are **per-node planning files**, not that tool.

## Adding another IDE later

Run `specyrd init` again with a different `--ai` value (e.g. `cursor` then `claude-code`). Use `--force` to overwrite stubs this tool installed.

## Manifest

[`manifest.json`](manifest.json) lists paths managed per `--ai` pack. Generated by specyrd {{SPECYRD_VERSION}}.
