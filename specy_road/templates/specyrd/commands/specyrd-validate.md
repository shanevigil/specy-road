---
description: Validate specy-road roadmap (JSON manifest + chunks, registry, schemas)
---

Thin stub — the **specy-road** CLI remains canonical. Generated by specyrd {{SPECYRD_VERSION}}.

Run from the **repository root**:

```bash
cd "$(git rev-parse --show-toplevel)"
```

```bash
specy-road validate
```

Equivalent:

```bash
specy-road validate
```

Agent load order: `AGENTS.md` at the repository root. Feature sheets live as flat `planning/*.md` files (see `planning/README.md`) — not the [Spec Kit](https://github.com/github/spec-kit) `specify` CLI.
