---
description: Add a roadmap node to a chunk via CLI
---

Thin stub — generated by specyrd {{SPECYRD_VERSION}}.

```bash
cd "$(git rev-parse --show-toplevel)"
specy-road add-node -h
```

See `docs/pm-workflow.md` and `docs/roadmap-authoring.md`.
