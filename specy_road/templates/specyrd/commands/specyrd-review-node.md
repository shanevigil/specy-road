---
description: Optional LLM advisory review of a roadmap node
---

Thin stub — generated by specyrd {{SPECYRD_VERSION}}.

Requires `pip install "specy-road[review]"` and environment variables (see `docs/pm-workflow.md`).

```bash
cd "$(git rev-parse --show-toplevel)"
specy-road review-node NODE_ID
```
