---
description: Regenerate roadmap.md from the merged JSON graph (or check drift)
---

Generated by specyrd {{SPECYRD_VERSION}}. From the **repository root**:

```bash
cd "$(git rev-parse --show-toplevel)"
```

Write generated views:

```bash
specy-road export
```

Equivalent:

```bash
python scripts/export_roadmap_md.py
```

CI drift check (exit 1 if `roadmap.md` differs from the merged graph):

```bash
python scripts/export_roadmap_md.py --check
```
