# Frozen Python deps for CI and pip-audit.
# Regenerate with Python 3.12 (matches .github/workflows/validate.yml):
#   pip install pip-tools
#   pip-compile requirements-ci.in -o requirements-ci.txt
#
-e .[dev]
