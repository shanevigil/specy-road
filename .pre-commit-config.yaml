# See https://pre-commit.com — install: pip install pre-commit && pre-commit install

repos:
  - repo: local
    hooks:
      - id: validate-roadmap
        name: validate roadmap and registry
        entry: python scripts/validate_roadmap.py
        language: system
        pass_filenames: false
        always_run: true
