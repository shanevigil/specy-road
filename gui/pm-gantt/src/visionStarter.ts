/**
 * Default body when creating `vision.md` from the PM UI (“Create vision.md”).
 * `specy-road init project` uses a different template (`templates/project/vision.md`);
 * both count as “needs completion” in `governance_completion.py`.
 */
export const VISION_STARTER = `# Vision

Describe the product vision for this repository. See README for how this relates to the roadmap.
`;
